# ---------------------------------------------------------
# PROVIDER SELECTION
# Options: 'google' (Free) or 'openai' (Paid)
# ---------------------------------------------------------
# LLM Options: 'google', 'openai' (Not used for generation in this assignment)
LLM_PROVIDER=google

# Embedding Options: 'huggingface' (Local/Free), 'openai' (Required for assignment)
EMBEDDING_PROVIDER=huggingface

# ---------------------------------------------------------
# HUGGINGFACE CONFIGURATION (Local / Free)
# ---------------------------------------------------------
HUGGINGFACE_EMBEDDING_MODEL=all-MiniLM-L6-v2

# ---------------------------------------------------------
# GOOGLE CONFIGURATION (Gemini)
# ---------------------------------------------------------
# Get your free key here: https://aistudio.google.com/
GOOGLE_API_KEY=your_api_key
GOOGLE_MODEL_NAME=gemini-2.5-flash
GOOGLE_EMBEDDING_MODEL=models/embedding-001

# ---------------------------------------------------------
# OPENAI CONFIGURATION (GPT-4o)
# ---------------------------------------------------------
# Leave this blank or fill it if you have a paid key
OPENAI_API_KEY=sk-proj-YOUR_OPENAI_KEY_IF_YOU_HAVE_ONE
OPENAI_MODEL_NAME=gpt-4o
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# ---------------------------------------------------------
# SHARED SETTINGS
# ---------------------------------------------------------
VECTOR_DB_TYPE=chroma
VECTOR_DB_PATH=data/vector_store
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
LOG_LEVEL=INFO